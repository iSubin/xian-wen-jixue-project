interface:
  display_name: "Xian Batch"
  short_description: "Goal-driven change batch execution"
  default_prompt: "Use $xian-batch to turn this goal into a governed sequence of changes."
policy:
  allow_implicit_invocation: false
